## FastAPI

Install $ pi p install "fastapi[standard]"
Verify installation through .venv package
    List 
Keep main.py 


check in video all the above/missing

## Endpoint
API and URL related
Consists of a path: "/exeample"
Accompanied by a HTTP-Method (GET, POST, PUT, DELETE)

## Decorator
Refers to the @ symbol
(Difference in how function executes)
Runs logic from external decorator function
    (Function over function)
Returns JSON data (automatic conversion)

ex: python 
@decorator
def test_function():


## URL
https://www.google.com/search?q=bananas

In this example we see a dynamic parameter
    q == query (just a variable name)
    ? == start of query
    What comes after = is Dynamic_Value

## Pydantic 
Uses schema (Defines logical data type structure)
Class based
Used for Data Validation
Faciliutates conversion of JSON --> Python objects
Best practices - (separated from its wown package)
Includes 'BaseModel' within class parameters



